# Where2Go · 项目计划与系统架构

> 面向“随心自由行”信息聚合工具：目的地推荐 → 路线交通 → 住宿 → 收藏对比与跳转预订。
> 阶段目标：先以**个人可部署、低成本、AI 深度参与**的方式跑通，稳定后再平滑迁移到 小程序 / 网站 / App。

---

## 一、总体思路与定位

把 4 个痛点模块抽象成一条“**决策漏斗**”：

```
选哪去(M1) → 怎么去(M2) → 住哪(M3) → 订不订/跳转(M4)
   │            │            │            └─ 只做信息聚合与跳转，不代订
   └──────┴────── 收藏/分类/对比 贯穿全程
```

**核心原则：**
- 应用本身**不碰资金、不做代订**，只聚合信息并 deep-link 到携程/Booking/地图 App —— 合规风险与运维成本最低。
- **价格类信息永远标注“口径 + 抓取时间 + 仅供参考”**（OTA 实时价 / AI 预估价分开）。
- 所有耗时采集（OTA、地图、AI 生成）走**异步 + 缓存**，不阻塞主流程。
- 数据源做成**适配器（Adapter）层**：先接入易获得的（官方 API + 搜索引擎 + AI 解析），日后替换/扩展零侵入。
- **AI LLM 做成可切换 Provider 抽象**（注册表 + 统一接口）：现状用既有 DeepSeek/Qwen key，将来产品化可平滑换 OpenAI/国产任选（ADR-002）。

---

## 二、推荐技术选型

### 应用平台（先个人部署 → 后迁移）
| 阶段 | 形态 | 理由 |
|---|---|---|
| 阶段一（现在） | **响应式 Web（PWA）**，自托管 | 一套代码电脑/手机/微信内均可打开，无需应用商店审核与小程序资质，最快迭代 |
| 阶段二 | **微信小程序** | 触达与分享最强；后端 API 原样复用，仅新增小程序壳 + 登录 |
| 阶段三 | **App（原生/Flutter/RN）** | 离线与推送；API 层同样复用 |

> 关键约束 X.08：前端形态可变，**业务与数据全在 API 层**，前端只做薄壳。

### 技术栈（推荐默认）
| 层 | 选型 | 说明 / 备选 |
|---|---|---|
| 后端 | **Python 3.12 + FastAPI** | AI/抓取生态最全，与 LLM 集成顺；备选 Node/Go |
| ORM/存储 | SQLAlchemy + **SQLite（起步）** | 单机个人部署足够；ORM 便于日后换 PostgreSQL |
| 任务队列 | **RQ / 简单后台线程**（起步） | 异步采集 + AI；量大再上 Celery |
| 前端 | **React 18 + Vite + TypeScript + Tailwind** | 已裁定为 React（ADR-001，神朱不熟编程交由实现侧自决） |
| 前端 UI/地图 | Ant Design / Element + **Leaflet + OSM 瓦片**（现均免费） | 高德 JS API 作可选升级（需 key） |
| AI 编排 | 现有 LLM（**DeepSeek / 阿里 Qwen API**）+ function-calling / ReAct | 经 **LLM Provider 抽象层**调用（ADR-002），key 集中在 .env；产品化可换；可挂 Hermes 做长任务 Agent |
| 抓取 | httpx + requests + BeautifulSoup；结构不稳时 **LLM 解析兜底** | 走既有代理，限频防封 |
| 部署 | **Docker Compose + Caddy/Nginx 反代**，跑在**绿联 DXP4800 Plus** NAS | 密钥入 .env |
| 质量 | pytest + ruff + pre-commit | 数据适配器必须有单测 |

---

## 三、系统架构

```
┌──────────────────────────── 前端 PWA ─────────────────────────────┐
│  目的地探索 / 详情  路线  住宿  收藏对比  预订跳转  地图(高德/Leaflet)│
└──────────────────────────────┬───────────────────────────────────┘
                               │ REST / WebSocket
┌────────────────────────── FastAPI 业务层 ─────────────────────────┐
│  Auth(user) · Place · Collection · Route · Accommodation · Plan  │
│  Compare聚合 · deep-link跳转 · 免责/口径标注                      │
└───────┬───────────────────┬───────────────────┬──────────────────┘
        │                   │                   │
┌───────▼────────┐  ┌───────▼────────┐  ┌───────▼──────────────┐
│ 数据适配器层     │  │ AI 编排层       │  │ 任务/缓存层           │
│ (Adapter)       │  │ (Agent)        │  │                     │
│ ·地图:高德/百度/  │  │ ·目的地推荐     │  │ ·RQ worker          │
│   Google/Bing    │  │ ·详情摘要生成   │  │ ·抓取结果落库缓存     │
│ ·OTA:携程/Booking│  │ ·非结构化解析   │  │ ·cron 定时刷新价格    │
│   /Airbnb        │  │ ·预算/行程方案   │  │ (可挂 Hermes cron)   │
│ ·POI/目的地库     │  │ function-calling│  │                     │
└───────┬────────┘  └───────┬────────┘  └───────────┬──────────┘
        │                  │                       │
        └──────────────┬───┘                       │
                 ┌─────▼─────┐               ┌─────▼─────┐
                 │ 存储 SQLite │◄──────────────│ 检索/缓存  │
                 │ (SQLAlchemy)│              └───────────┘
                 └───────────┘
  外部：OSRM 路由 · Nominatim/Overpass/OSM POI · 携程/Booking/Airbnb 网页 · LLM API · 代理
```

### 依赖方向（单向）
前端 → 业务 API → **适配器/AI/任务** → 存储 + 外部。**适配器接口先行**，各来源只依赖抽象接口，替换来源不改业务。

---

## 四、数据模型（核心实体）

```
User(id, nickname, oauth)
Place(id, name, category, lat, lng, city, country, intro, rating, images,
      dist_band, detail_json)          # detail_json 存“类别专属结构”（自然/美食/滑雪/运动）
Collection(id, user_id, place_id, note, cat_id)
CollectionCat(id, user_id, name, auto_rule)
Route(id, user_id, place_id, from_name, mode, duration_min, cost_cny,
      steps_json, provider, source_url, fetched_at)
Accommodation(id, place_id, name, type, price_cny, currency, price_kind,  # 现阶段=AI预估；产品化=OTA实时|AI预估
              source(ctrip/booking/airbnb), url, rating, images, fetched_at)
SavedAccom(id, user_id, accommodation_id, note)
Plan(id, user_id, name, place_id, route_id, accom_ids, persons, total_cny)  # M4.03 行程方案
```

- `price_kind` + `fetched_at` 落实“价格口径 + 新鲜度”这一硬约束。
- `Route.steps_json / Place.detail_json` 用 JSON 字段承载异构详情，避免过早建一堆空表。

---

## 五、核心难点与方案（数据源：免费优先）

> **现状无高德/Google key** → POC 阶段已验证可用的**免费、无需申请**源如下（2026-09-07 实测）：
> - **驾车路线**：OSRM 公共 API（router.project-osrm.org / routing.openstreetmap.de，全球，返回耗时/距离）✓
> - **地理编码/逆地理**：Nominatim（OSM）✓
> - **目的地 POI/滑雪场等**：Overpass API（OSM 标签检索，按距离圈取）✓
> - 局限：OSRM 只覆盖**驾车**；国内无免费公共交通/实时路况/OTA 房价接口。

| 难点 | 方案 |
|---|---|
| 驾车路线（M2） | ✅ **OSRM 公共 API 已验证并落地**（`backend/data_sources/osrm.py`，含备选端点切换）→ 产品化再评估高德/Google |
| 目的地库冷启动（M1） | **Overpass（OSM 标签）+ Nominatim** 按分类/距离圈拉真实 POI；国内 POI 覆盖偏弱时以 AI 结合搜索结果补强；人工过种子数据 |
| 公共交通/高铁/公交 | 免费接口无实时价与班次。策略：**给出“驾车保底”+ AI 综合公开信息给出行方式建议与预估**；火车票/机票最终跳 12306/OTA 以官方为准（M4.07） |
| OTA 房价 | 无官方免费 key。策略：① 搜索引擎+页面抓取做参考；② AI 综合多源给“预估参考价”并**明确标注**；点进去核实时价。不做硬破解反爬；产品化再谈联盟接口 |
| 反爬与合规 | 限频、respect robots、结果缓存复用；来源落库可审计；页脚免责“不代订，仅供参考” |
| AI 幻觉 | 事实类字段（价格/距离/耗时）绑定结构化来源；LLM 只做“推荐、简介、解析、总账”，并附来源链接 |
| 成本 | 结果缓存 + 热度高的目的地 cron 预取，避免每次实时调 AI；免费源额度有限，需限频 |

---

## 六、项目结构（建议）

```
Where2Go-Planner/
├─ docs/            需求/架构/验收（01/02 即此处）
├─ backend/
│  ├─ app/  main.py · api/(v1模块) · core/(config,db,auth)
│  │   ├─ adapters/   map/ ota/ poidb/        # 来源抽象+实现
│  │   ├─ ai/         recommender/ summarizer/ parser/
│  │   └─ models/ services/ schemas/
│  ├─ workers/       rq 任务：抓取、AI、刷新
│  └─ tests/
├─ frontend/         React+Vite+PWA
├─ scripts/          种子数据、cron 刷新、迁移小程序等
├─ docker-compose.yml
└─ .env.example     密钥清单（高德/地图、OTA、LLM、代理）
```

---

## 七、分阶段实施计划

| 阶段 | 周期 | 交付物 / 里程碑 |
|---|---|---|
| **0 · 定稿与 POC** | ✅ **已完成**（2026-09-08,代码见 `backend/` + TASK-001）| 数据源层已验证:OSRM/Nominatim/Overpass 真实链路 6/6 通过、26 单测全绿;已推送 GitHub（main 分支）。下一步:阶段1 FastAPI 骨架 + M1 雏形 |
| **1 · 骨架 + M1** | 2–3 周 | 后端框架 + DB + 前端 PWA 壳；M1 目的地选择/分类/距离/AI 推荐/收藏(自动+手动分类) |
| **2 · M2 + M3** | 2–3 周 | M2 路线（高德 + 海外适配器、收藏）；M3 住宿（OTA 抓取做 AI 预估参考价、收藏）；实时价与跨 OTA 比价留产品化阶段 |
| **3 · M4 + 打磨** | 1–2 周 | M4 收藏统一面板/方案组合/总账/一键跳转预订；免责口径、缓存、端到端验收(见清单末验收口径) |
| **4 · 迁移** | 迭代 | 稳定后转**微信小程序**（API 复用）；再按需 App；加用户登录与多设备同步 |

> 建议每阶段结束跑一遍《01-功能需求清单》对应 P0 验收项再进入下一阶段。

---

## 八、风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| OTA 反爬/数据不可得 | 高 | 尽早 POC 验证；多来源 + AI 预估参考价兜底；不代订只跳转 |
| AI 幻觉误导决策 | 中 | 事实字段绑定结构化源；价格/距离/耗时全部标口径与时间 |
| 地图/OTA API 收费与配额 | 中 | 结果缓存 + 低配额策略 + 多 key/多来源轮换 |
| 合规（数据来源、跳转） | 中 | 只聚合+跳转不代订；来源可审计；页脚免责 |
| 范围蔓延 | 中 | 严格按 P0 做 MVP，P1/P2 冻结在清单 |
| 个人部署维护负担 | 低 | Docker Compose 一键起；冷门功能关闭以减成本 |

---

## 九、部署方案（阶段一，个人）

```
绿联 DXP4800 Plus（Docker）
  ├─ backend (FastAPI)     .env: LLM key、代理（OSRM/Nominatim/Overpass 免费无需 key）
  ├─ frontend (静态站)     由 Caddy/Nginx 托管 + 反代 /api
  ├─ worker (RQ)           异步抓取 + AI
  └─ caddy                 自动 HTTPS + 域名
数据卷挂载 sqlite 文件 → 备份即复制
迁移微信小程序：新增小程序前端 + 复用 backend，微信登录换入 X.01 用户体系
```

**成本预估（个人）**：主要为 LLM token 与地图/OTA API 配额，配缓存后可控制在很低的量级；部署零月费（绿联 NAS 本地，Docker 运行）。

---

## 十、架构决策记录（ADR）
| 编号 | 决策 | 理由 / 备注 |
|---|---|---|
| ADR-001 | 前端用 **React**（Vite+TS+Tailwind） | 神朱交由实现侧自决，助手裁定 React，生态组件丰富 |
| ADR-002 | **LLM Provider 抽象层**，key 可切换 | 现状用既有 DeepSeek/Qwen；产品化可换 OpenAI 等，改动仅配置层 |
| ADR-006 | POC 数据源采用**免费无需 key**：OSRM(驾车)/Nominatim(编码)/Overpass(POI) | 神朱暂无高德/Google key；三者 2026-09-07 实测通。局限：仅驾车、无实时路况/公交/OTA 价 |
| ADR-007 | 公共交通与 OTA 价**不做实时抓取**，走“驾车保底 + AI 预估 + 跳转 12306/OTA 以官方为准” | 免费无此数据；降复杂度、合规优先 |
| ADR-008 | 数据源层已实现(见 `backend/data_sources/`) | OSRM 驾车 + Nominatim 正/逆编码 + Overpass 分类 POI;真实 6/6、单测 26/26(2026-09-08) |
| ADR-009 | Overpass 用**端点链 + 重试降级**应对公共实例 504 | 实测 `z.overpass-api.de`/`maps.mail.ru` 可作降级;`osm.ch` 无数据、`private.coffee` 陈旧未纳入 |
| ADR-003 | 阶段一住宿价**仅 AI 预估参考价** | 依意见；OTA 实时价/比价推迟产品化，页面必须标口径与时间 |
| ADR-004 | M4 只做聚合 + deep-link 跳转，不代订 | 合规、低成本；地图 API 不承接下单 |
| ADR-005 | 部署主机 = **绿联 DXP4800 Plus**（Docker） | 更正原文档“群晖”；架构不受影响 |

